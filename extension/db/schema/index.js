'use strict';

// =============================================================================

const { AuthorizationSchema } = require('./authorization');
const { ChannelConfigSchema } = require('./channel_config');

// =============================================================================

module.exports = {
  AuthorizationSchema,
  ChannelConfigSchema
};
