'use strict';

// =============================================================================

const { AuthorizationSchema } = require('./authorization');
const { UserSchema } = require('./user');

// =============================================================================

module.exports = {
  AuthorizationSchema,
  UserSchema
};
